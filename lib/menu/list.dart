import 'package:flutter/material.dart';

class ListOfPeople extends StatelessWidget {
  const ListOfPeople({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(" this is list of friend"),
    );
  }
}
